<Window x:Class="RestaurantApp.Views.AuthWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RestaurantApp.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Autentificare" Height="600" Width="400"
        WindowStartupLocation="CenterScreen"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="{Binding WindowTitle}"
                   Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"/>

        <!-- Form -->
        <StackPanel Grid.Row="1">
            <!-- Email -->
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Email"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,8"/>

            <!-- Parola -->
            <PasswordBox x:Name="PasswordBox"
                         materialDesign:HintAssist.Hint="Parola"
                         Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                         Margin="0,8"/>

            <!-- Confirma parola (doar pentru inregistrare) -->
            <PasswordBox x:Name="ConfirmPasswordBox"
                         materialDesign:HintAssist.Hint="Confirma parola"
                         Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                         Margin="0,8"
                         Visibility="{Binding IsRegisterMode, Converter={StaticResource BooleanToVisibilityConverter}}"/>

            <!-- Nume si prenume (doar pentru inregistrare) -->
            <Grid Visibility="{Binding IsRegisterMode, Converter={StaticResource BooleanToVisibilityConverter}}"
                  Margin="0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="0"
                         Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="Prenume"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="0,0,4,0"/>

                <TextBox Grid.Column="1"
                         Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}"
                         materialDesign:HintAssist.Hint="Nume"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="4,0,0,0"/>
            </Grid>

            <!-- Numar de telefon (doar pentru inregistrare) -->
            <TextBox Text="{Binding PhoneNumber, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="Numar de telefon"
                     Style="{StaticResource MaterialDesignOutlinedTextBox}"
                     Margin="0,8"
                     Visibility="{Binding IsRegisterMode, Converter={StaticResource BooleanToVisibilityConverter}}"/>

            <!-- Mesaj de eroare -->
            <TextBlock Text="{Binding ErrorMessage}"
                     Foreground="Red"
                     TextWrapping="Wrap"
                     Margin="0,8"
                     Visibility="{Binding HasError, Converter={StaticResource BooleanToVisibilityConverter}}"/>

            <!-- Buton de autentificare/inregistrare -->
            <Button Content="{Binding ActionButtonText}"
                    Command="{Binding ActionCommand}"
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Margin="0,16,0,0"/>

            <!-- Link pentru schimbarea modului -->
            <Button Content="{Binding ToggleModeText}"
                    Command="{Binding ToggleModeCommand}"
                    Style="{StaticResource MaterialDesignFlatButton}"
                    Margin="0,8"/>
        </StackPanel>
    </Grid>
</Window> 