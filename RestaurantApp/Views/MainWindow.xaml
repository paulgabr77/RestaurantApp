<Window x:Class="RestaurantApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RestaurantApp.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Restaurant App" Height="720" Width="1280"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Top Menu -->
        <materialDesign:ColorZone Mode="PrimaryMid" Padding="16" materialDesign:ShadowAssist.ShadowDepth="Depth2">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <TextBlock VerticalAlignment="Center" Margin="0,0,16,0">
                        <Run Text="Restaurant App" FontSize="20"/>
                    </TextBlock>
                </StackPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <Button Style="{StaticResource MaterialDesignFlatButton}" Margin="8,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Account" Margin="0,0,8,0"/>
                            <TextBlock Text="Cont"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </DockPanel>
        </materialDesign:ColorZone>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Categories List -->
            <materialDesign:Card Grid.Column="0" Margin="16" Padding="8">
                <DockPanel>
                    <TextBox DockPanel.Dock="Top"
                             Margin="0,0,0,8"
                             materialDesign:HintAssist.Hint="Caută..."
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"/>
                    <ListBox x:Name="CategoriesList"
                            materialDesign:RippleAssist.IsDisabled="True">
                        <ListBoxItem Content="Toate Categoriile"/>
                        <ListBoxItem Content="Mic Dejun"/>
                        <ListBoxItem Content="Aperitive"/>
                        <ListBoxItem Content="Supe și Ciorbe"/>
                        <ListBoxItem Content="Feluri Principale"/>
                        <ListBoxItem Content="Deserturi"/>
                        <ListBoxItem Content="Băuturi"/>
                    </ListBox>
                </DockPanel>
            </materialDesign:Card>

            <!-- Dishes Grid -->
            <ScrollViewer Grid.Column="1" Margin="0,16,16,16">
                <ItemsControl x:Name="DishesList">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <materialDesign:Card Margin="8" Width="280" Height="320">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="180"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <Image Source="{Binding MainImagePath}" 
                                           Stretch="UniformToFill"/>

                                    <StackPanel Grid.Row="1" Margin="16">
                                        <TextBlock Text="{Binding Name}"
                                                 Style="{StaticResource MaterialDesignHeadline6TextBlock}"/>
                                        <TextBlock Text="{Binding Description}"
                                                 Style="{StaticResource MaterialDesignBody1TextBlock}"
                                                 TextWrapping="Wrap"
                                                 Margin="0,8,0,8"/>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Price, StringFormat='{}{0:C}'}"
                                                     Style="{StaticResource MaterialDesignBody1TextBlock}"
                                                     VerticalAlignment="Center"/>
                                            <Button Grid.Column="1"
                                                    Style="{StaticResource MaterialDesignFloatingActionMiniButton}"
                                                    materialDesign:ButtonAssist.CornerRadius="16">
                                                <materialDesign:PackIcon Kind="Plus"/>
                                            </Button>
                                        </Grid>
                                    </StackPanel>
                                </Grid>
                            </materialDesign:Card>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window> 